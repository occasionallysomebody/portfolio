{
  "name": "moonshot-709-portfolio",
  "version": "1.0.0",
  "description": "Minimalist photography portfolio with HEIC support",
  "main": "scripts/optimize.js",
  "scripts": {
    "optimize": "node ./scripts/optimize.js",
    "manifest": "node ./scripts/get-images.js",
    "styles": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "include-nav": "node ./scripts/include-nav.js",
    "posts:build": "node ./scripts/md-to-json.js",
    "build": "pip install -r requirements.txt && npm run heic-convert && npm run posts:build && npm run include-nav && npm run optimize && npm run manifest && npm run styles"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "heic-convert": "^2.1.0",
    "sharp": "^0.33.2",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "lightningcss": "^1.24.1",
    "gray-matter": "^4.0.3"
  }
}