{
  "name": "moonshot-709-portfolio",
  "version": "1.0.0",
  "description": "Minimalist photography portfolio with HEIC support",
  "main": "scripts/optimize.js",
  "scripts": {
    "heic-convert": "python scripts/convert_heic.py",
    "optimize": "node src/scripts/optimize.js",
    "manifest": "node src/scripts/get-images.js",
    "styles": "tailwindcss -i ./src/styles/input.css -o ./dist/output.css --minify",
    "include-SE": "node src/scripts/include-semanticElements.js",
    "posts:build": "node src/scripts/md-to-json.js",
    "build": "pip install -r pythonRequirements.txt && npm install @tailwindcss/cli gray-matter marked && npm run posts:build && npm run include-SE && npm run optimize && npm run manifest && npm run styles"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "fs-extra": "^11.2.0",
    "marked": "^17.0.1",
    "sharp": "^0.33.2",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "lightningcss": "^1.24.1"
  }
}
