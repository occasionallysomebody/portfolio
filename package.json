{
  "name": "moonshot-709-portfolio",
  "version": "1.0.0",
  "description": "Minimalist photography portfolio with HEIC support",
  "main": "scripts/optimize.js",
  "scripts": {
    "heic-convert": "python scripts/convert_heic.py",
    "optimize": "node scripts/optimize.js",
    "manifest": "node scripts/get-images.js",
    "styles": "npx tailwindcss -i ./styling/input.css -o ./styling/output.css --minify",
    "include-nav": "node scripts/include-nav.js",
    "posts:build": "node scripts/md-to-json.js",
    "build": "pip install -r pythonRequirements.txt && && npm install gray-matter marked && npm run posts:build && npm run include-nav && npm run optimize && npm run manifest && npm run styles"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "sharp": "^0.33.2",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "lightningcss": "^1.24.1"
  }
}
