{
  "name": "moonshot-709-portfolio",
  "version": "1.0.0",
  "description": "Minimalist photography portfolio with HEIC support",
  "main": "scripts/optimize.js",
  "scripts": {
    "heic-convert": "python src/scripts/convert_heic.py",
    "posts:build": "node src/scripts/md-to-json.js",
    "include-SE": "node src/scripts/include-semanticElements.js",
    "optimize": "node src/scripts/optimize.js",
    "manifest": "node src/scripts/get-images.js",
    "styles": "tailwindcss -i ./src/styling/input.css -o ./dist/assets/output.css --minify",
    "build": "pip install -r pythonRequirements.txt && npm install @tailwindcss/cli gray-matter marked && npm run heic-convert && npm run posts:build && npm run include-SE && npm run optimize && npm run manifest && npm run styles"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "fs-extra": "^11.2.0",
    "marked": "^17.0.1",
    "sharp": "^0.33.2",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "lightningcss": "^1.24.1"
  }
}
